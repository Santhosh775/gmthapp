import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UnitedarabemiratesPage } from './unitedarabemirates.page';

describe('UnitedarabemiratesPage', () => {
  let component: UnitedarabemiratesPage;
  let fixture: ComponentFixture<UnitedarabemiratesPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(UnitedarabemiratesPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
